# Set default behavior, in case files match no other rules
* text=auto

# Explicitly declare text files you want to always be normalized and converted to LF on checkin
*.js text eol=lf
*.json text eol=lf
